@using Aurora.Core
@model IEnumerable<Vehiculo>

@{
    ViewData["Title"] = "Empresa";
    Layout = "_EmpresaLayout";
}

@section Styles {
    <link rel="stylesheet" href="~/css/_sidebar.css" />
    <link rel="stylesheet" href="~/css/_table.css" />
}

<div class="main-content">
    <div class="menu">
        <a asp-action="AgregarVehiculo" asp-controller="Empresa">Agregar nuevo vehiculo</a>
        <a asp-action="AsignarVehiculoAConductor" asp-controller="Empresa">Asignar Vehiculo a Conductor</a>
    </div>

    <div class="container">
        <h1>Gestión de Flota</h1>
        <div class="table-container">
            <table class="Content-table">
                <thead class="table-header">
                    <tr>
                        <th>Codigo</th>
                        <th>Tipo de Vehiculo</th>
                        <th>Matricula</th>
                        <th>Capacidad Maxima (KG)</th>
                        <th>Estado actual</th>
                    </tr>
                </thead>
                <tbody>
                    @if (!Model.Any())
                    {
                        <tr>
                            <td colspan="6" class="text-center">No hay Vehiculos.</td>
                        </tr>
                    }
                    else
                    {
                        @foreach (var flota in Model)
                        {
                            <tr>
                                <td data-label="Código">@flota.IdVehiculo</td>
                                <td data-label="Tipo">@flota.Tipo</td>
                                <td data-label="Matricula">@flota.Matricula</td>
                                <td data-label="Capacidad">@(flota.CapacidadMax + "Kg")</td>
                                <td data-label="Estado">@(flota.Estado ? "Disponible" : "En viaje")</td>
                            </tr>
                        }
                    }
            </tbody>
            </table>
        </div>
    </div>
</div>
